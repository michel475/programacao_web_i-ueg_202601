TODO gerar documetnação do projeto

alterar o método de criação para retornar 404 quando o usuário já existir

Tratar campos obrigatórios (de forma automatica)

3. no endpoint de remoção (delete) deve retornar 404 caso o id informado não exista

4. no endpoint de remoção deve retornar o registro removido no corpo de resposta

5. endpoint desativar deve tratar a exceção de usuário não encontrado.

6. implementar ação de ediçao que deve receber um DTO com os dados nome email que deve ser validado a unicidade do mesmo

